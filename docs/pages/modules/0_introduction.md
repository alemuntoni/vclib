# VCLib Modules

The VCLib library is organized in several modules, each one of them providing a set of functionalities that can be used independently from the others. The modules are organized in a way that the user can include only the modules that are needed, without including the whole library.

All the modules are written in C++20, and can be built with CMake.

## Core

## External

## Processing

## Render

## Bindings